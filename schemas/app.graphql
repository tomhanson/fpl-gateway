# import PlayerData from "./players.graphql"
type Query {
  players(
    per_page: Int!
    page: Int!
    sortOption: SortOption
    sortOrder: SortOrder
    filter: String
  ): [PlayerData]
}

enum SortOrder {
  ASC
  DESC
}
