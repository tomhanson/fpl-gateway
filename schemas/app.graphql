# import PlayerData from "./players.graphql"
# import TeamData from "./teams.graphql"
type Query {
  players(
    per_page: Int!
    page: Int!
    sortOption: SortOption
    sortOrder: SortOrder
    filter: String
  ): [PlayerData]
  teams: [TeamData]
}

enum SortOrder {
  ASC
  DESC
}
