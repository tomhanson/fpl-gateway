# import PlayerData from "./players.graphql"
# import TeamData from "./teams.graphql"
type Query {
  players(
    perPage: Int!
    page: Int!
    sortOption: SortOption
    sortOrder: SortOrder
    filter: String
  ): [PlayerData]
  teams: [TeamData]
}

enum SortOrder {
  ASC
  DESC
}
